{
    "type": "object",
    "properties": {
        "genre_id": {
            "required": true,
            "example": "animation",
            "type": "string"
        },
        "direction": {
            "required": false,
            "enum": [
                "asc",
                "desc"
            ],
            "example": "asc",
            "type": "string"
        },
        "filter": {
            "required": false,
            "enum": [
                "country",
                "my_region"
            ],
            "type": "string"
        },
        "page": {
            "required": false,
            "example": 1,
            "type": "number"
        },
        "per_page": {
            "required": false,
            "example": 10,
            "type": "number"
        },
        "query": {
            "required": false,
            "example": "Stop motion",
            "type": "string"
        },
        "sort": {
            "required": false,
            "enum": [
                "alphabetical",
                "date",
                "name",
                "publish.time",
                "videos"
            ],
            "type": "string"
        }
    }
}